<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.king.game.dao.VoteRecordMapper">
  <resultMap id="BaseResultMap" type="com.king.game.entity.VoteRecord">
    <result column="id" jdbcType="BIGINT" property="id" />
    <result column="vote_id" jdbcType="BIGINT" property="voteId" />
    <result column="user_id" jdbcType="BIGINT" property="userId" />
    <result column="vote_time" jdbcType="TIMESTAMP" property="voteTime" />
  </resultMap>
  <insert id="insert" parameterType="com.king.game.entity.VoteRecord">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into vote_record (vote_id, user_id, vote_time
      )
    values (#{voteId,jdbcType=BIGINT}, #{userId,jdbcType=BIGINT}, #{voteTime,jdbcType=TIMESTAMP}
      )
  </insert>
  <select id="selectAll" resultMap="BaseResultMap">
    select id, vote_id, user_id, vote_time
    from vote_record
  </select>

  <select id="selectMyRecord" resultMap="BaseResultMap" parameterType="java.util.Map">
    select id, vote_id, user_id, vote_time
    from vote_record
    where user_id = #{userId,jdbcType=BIGINT} and vote_id = #{voteId,jdbcType=BIGINT}
  </select>
</mapper>